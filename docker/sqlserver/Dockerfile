# Microsoft SQL Server 2022 (Linux). Runtime config via env: ACCEPT_EULA, MSSQL_SA_PASSWORD, MSSQL_PID.
# Schema is applied separately (e.g. db-init job or migration) using backend/src/init.sql.
FROM mcr.microsoft.com/mssql/server:2022-latest
