PORT=5000
DB_HOST=localhost
DB_PORT=1433
DB_NAME=poems_db
DB_USER=sa
DB_PASSWORD=YourStrong!Passw0rd
DB_INSTANCE=
DB_DRIVER=
DB_ENCRYPT=false
DB_TRUST_SERVER_CERTIFICATE=true
DB_DEBUG=false

# When true, poem/payment data can fall back to backend/data/poems-store.json if SQL Server is down.
# User login, registration, admin login, and GET /api/users/me always use SQL Server (never the JSON file).
DB_ALLOW_LOCAL_FALLBACK=false

JWT_SECRET=change-me-in-production
